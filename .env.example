# Database (required for dev and seed)
# DATABASE_URL=postgres://user:password@host:5432/picker
# Or: PG_HOST, PG_PORT, PG_USER, PG_PASSWORD, PG_DATABASE

# OpenAI (source extract, content generate, script mutate). Use KEY=value only (no "export ").
# OPENAI_API_KEY=sk-...

# Entra ID (Azure AD) OAuth 2.0 — required for API auth (all routes except GET /).
# ENTRA_TENANT_ID=<tenant-id>
# ENTRA_CLIENT_ID=<application-client-id>  # token aud
# ENTRA_ISSUER=https://login.microsoftonline.com/<tenant-id>/v2.0  # optional
# Test only: TEST_SKIP_ENTRA_AUTH=1 skips Bearer verification (do not set in prod).

# Identity index (PII); do not commit real data. Optional path to actual file (default: shared/record/reference/identity-index.toml).
# IDENTITY_INDEX_PATH=./secrets/identity-index.toml

# Lexis source metadata — copyright-sensitive; do not commit (store.md §V).
# Value = JSON: { "type": "book", "metadata": { "title": "...", "keywords": ["short name"], "total_entries": N, "words_per_day": N, "total_days": N } }
# title = full book name (utterance match); keywords = optional short names for matching.
# LEXIS_SOURCE_META_MIDDLE_BASIC={"type":"book","metadata":{"title":"...","total_entries":N,"words_per_day":N,"total_days":N}}
# LEXIS_SOURCE_META_MIDDLE_INTERMEDIATE={"type":"book","metadata":{"title":"...","total_entries":1200,"words_per_day":30,"total_days":40}}
# LEXIS_SOURCE_META_HIGH_BASIC={"type":"book","metadata":{"title":"...","total_entries":1200,"words_per_day":40,"total_days":30}}
