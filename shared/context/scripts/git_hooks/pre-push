#!/bin/sh
# Pre-push hook: run todo-check before pushing.
# Install: cp shared/context/scripts/git_hooks/pre-push .git/hooks/pre-push && chmod +x .git/hooks/pre-push
# Git runs hooks from repo root.

set -e
cd "$(git rev-parse --show-toplevel)"
deno task todo-check
