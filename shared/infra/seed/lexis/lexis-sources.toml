# Lexis source seed: serialization rationale only.
# Actual metadata (title, total_entries, words_per_day, total_days) lives in .env
# per store.md §V. Env var = JSON object for payload: source_id, type, metadata.

[[source]]
source_id = "wordmaster-middle-intermediate"
env_var = "LEXIS_SOURCE_META_WORDMASTER_MIDDLE_INTERMEDIATE"
