{
  "compilerOptions": {
    "lib": [
      "deno.window",
      "deno.unstable"
    ]
  },
  "fmt": {
    "lineWidth": 80
  },
  "tasks": {
    "dev": "deno run -A --watch main.ts",
    "build": "deno run -A npm:vite build",
    "start": "deno run -A main.ts",
    "test": "deno test -A",
    "test:e2e-record": "deno run -A shared/prompt/scripts/run-e2e-record.ts",
    "scope-check": "deno run --allow-read shared/prompt/scripts/check-scope.ts",
    "scope-discovery": "deno run --allow-read shared/prompt/scripts/scope-discovery.ts",
    "seed:ontology": "deno run -A shared/infra/seed/run-ontology.ts",
    "type-check-policy": "deno run --allow-read shared/prompt/scripts/check-type-policy.ts",
    "migrate:old-to-data": "deno run -A shared/prompt/scripts/migrate-old-to-data.ts",
    "naming-layer-check": "deno run --allow-read shared/prompt/scripts/check-naming-layer.ts",
    "ts-filename-check": "deno run --allow-read shared/prompt/scripts/check-ts-filename.ts",
    "sql-filename-check": "deno run --allow-read shared/prompt/scripts/check-sql-filename.ts",
    "line-length-check": "deno run --allow-read shared/prompt/scripts/check-line-length.ts",
    "boundary-check": "deno run --allow-read shared/prompt/scripts/check-domain-boundary.ts",
    "dependency-check": "deno run --allow-read shared/prompt/scripts/check-domain-deps.ts",
    "audit": "deno audit",
    "pre-push": "deno lint && deno fmt --check && deno task line-length-check && deno task ts-filename-check && deno task sql-filename-check && deno task test && deno task scope-check && deno task boundary-check && deno task dependency-check && deno task type-check-policy && deno audit"
  },
  "lint": {
    "plugins": [
      "./shared/prompt/scripts/function-length-lint-plugin.ts"
    ],
    "exclude": [
      "node_modules",
      "**/*.test.ts",
      "**/*.spec.ts",
      "**/*_test.ts"
    ],
    "rules": {
      "exclude": [
        "ban-unused-ignore"
      ]
    }
  },
  "imports": {
    "#system/": "./system/",
    "#shared/": "./shared/",
    "@std/assert": "jsr:@std/assert@1",
    "@std/front-matter": "jsr:@std/front-matter@^1.0.9",
    "@std/toml": "jsr:@std/toml@^1.0.11",
    "@std/uuid": "jsr:@std/uuid@^1.1.0",
    "hono": "jsr:@hono/hono@^4",
    "zod": "jsr:@zod/zod@^4",
    "ts-morph": "jsr:@ts-morph/ts-morph@^27",
    "vite": "npm:vite@^6",
    "@db/postgres": "jsr:@db/postgres@^0.19",
    "phonemize": "npm:phonemize@^1.1",
    "wink-nlp": "npm:wink-nlp@^2",
    "compromise": "npm:compromise@^14"
  }
}
