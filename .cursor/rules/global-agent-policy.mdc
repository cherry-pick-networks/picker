---
description: |
  DEPRECATED: Replaced by split rules. Use global-core (always), plus
  global-code-*, global-docs-*, system-*, global-dependency per context.
alwaysApply: false
---

This rule is deprecated. Follow instead:
- **global-core.mdc** — §C, §I, §O (always)
- **global-code-session.mdc**, **global-code-format.mdc**, **global-code-style.mdc**,
  **global-code-feature.mdc** — when editing .ts
- **global-docs-format.mdc**, **global-docs-prompt.mdc** — when editing .md/.mdc
- **system-document-boundary.mdc**, **system-agent-boundary.mdc**,
  **system-directory-structure.mdc** — when editing todo or system/
- **global-dependency.mdc** — when editing deno.json/package.json
Full section refs (§7, §A, §B, …) are in `shared/prompt/store.md`.
